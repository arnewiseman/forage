An immediate on/off toggle — use only when the change applies instantly (no Save button).

```jsx
<Switch label="Show only roles hiring now" defaultChecked />
```

If the setting needs saving, use `Checkbox` instead.

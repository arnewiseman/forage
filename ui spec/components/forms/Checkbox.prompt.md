Square multi-select control, 20px box with a fir-green checked fill.

```jsx
<Checkbox label="Remote-friendly only" description="Hides roles that require on-site work" />
<Checkbox label="All sectors" indeterminate checked={false} onChange={toggleAll} />
```

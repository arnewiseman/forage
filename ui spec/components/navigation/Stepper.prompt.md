Tracks position in Forage's linear flow: describe → match → target → gap → roles.

```jsx
<Stepper current={2} steps={["Describe", "Match", "Target", "Gap", "Roles"]} />
```

Completed steps get a moss check, the current step a filled fir circle. Keep labels to one word.

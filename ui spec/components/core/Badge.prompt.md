A small uppercase status marker — read-only, never clickable.

```jsx
<Badge tone="positive" icon="check">You have this</Badge>
<Badge tone="caution">Gap</Badge>
```

Tones: neutral, brand, positive, caution, critical, info, solid. Use `Tag` instead when the thing is a selectable or removable value.

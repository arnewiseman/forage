A square, label-less button for toolbar and card affordances.

```jsx
<IconButton icon="bookmark" label="Save this role" variant="outline" />
```

`label` is required (it becomes both aria-label and the tooltip). Variants: `ghost` (default), `outline` (print-shadow hover), `solid`.
